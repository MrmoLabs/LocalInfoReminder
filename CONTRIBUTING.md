# 贡献指南

感谢你参与 LocalInfoReminder。项目面向通用、本地、用户可控的屏幕信息识别与提醒场景。所有贡献都必须维持这一定位和下面的安全边界。

## 可以贡献的内容

- 通用的屏幕区域识别、OCR、计时和提醒能力
- 界面、无障碍、性能、稳定性和本地化改进
- 不包含第三方受保护素材的通用配置模板
- 自动化测试、构建流程和文档改进
- 隐私保护、安全加固和依赖治理

## 不接受的内容

项目不会接受或协助开发以下功能：

- 注入、附加、调试或控制第三方进程
- 读取、修改或推断第三方进程内存、客户端文件或私有数据
- 截获、伪造、重放或修改第三方网络通信
- 绕过反作弊、访问控制、技术保护措施或检测机制
- 向第三方软件发送自动键盘、鼠标、触摸或控制器输入
- 一键宏、自动操作、无人值守执行、批量账号控制或代操作
- 以取得不公平优势、破坏公平性或违反第三方服务规则为目的的识别规则
- 未经授权使用第三方商标、Logo、角色、截图、音频或其他受保护素材
- 宣称项目获得第三方官方授权、认可或安全保证的文案

全局输入监听只能用于接收用户主动输入并更新本项目内部状态。一次用户输入不得被转换为发送给第三方软件的一组操作。

## 配置和模板要求

新增的公共模板和文档示例应使用中性名称、虚构数据和项目自有资源。请使用 `Group A`、`Event A`、`Target A` 等通用名称，不要在公共模板中新增指向特定游戏、平台、角色、技能或玩法的默认规则。根目录 `config.json` 是维护者用于回归验证的可修改运行配置，不应被视为框架默认能力或官方适配声明。

如果模板用于说明某类真实场景，请确保：

1. 不包含第三方受保护素材或账号数据。
2. 不暗示官方合作或认可。
3. 不包含自动操作或规避检测能力。
4. 明确要求使用者自行遵守相关平台规则。

## 提交前检查

1. 使用 Python 3.12 创建项目虚拟环境并安装开发依赖。
2. 运行 `run_tests.bat`，确保默认测试通过。
3. 确认没有提交日志、状态、截图、构建产物、个人路径或凭据。
4. 搜索新增内容中的第三方品牌名称和素材来源，确认确有必要且具备使用权限。
5. 对影响发布包的改动运行 PyInstaller 构建并验证实际程序启动。

## 问题和安全报告

普通缺陷和功能建议可以通过项目问题追踪渠道提交。涉及凭据泄露、未授权数据访问、技术措施规避或其他安全风险时，请不要公开利用细节；应先联系维护者进行负责任披露。

提交贡献即表示你有权提供相关内容，并同意按照项目的 Apache License 2.0 许可该贡献。
